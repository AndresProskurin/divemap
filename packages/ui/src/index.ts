/**
 * @divemap/ui — shared components for web + mobile.
 *
 * Design tokens are the source of truth for all colors, typography, spacing
 * and component styling. Never hardcode values; import from here instead.
 */

export {
  colors,
  typography,
  radius,
  shadows,
  spacing,
  animation,
  components,
  tailwindExtend,
} from './tokens'
