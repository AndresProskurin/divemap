# Metro inlines only EXPO_PUBLIC_* vars. Copy to .env (gitignored) and fill in.
EXPO_PUBLIC_SUPABASE_URL=https://sdinzyrebuyjrhrnqldy.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_MAPBOX_TOKEN=pk....
# Secret Mapbox token with Downloads:Read scope — needed only to BUILD the app
# (native SDK download at pod install). Create at account.mapbox.com/access-tokens.
RNMAPBOX_DOWNLOAD_TOKEN=sk....
